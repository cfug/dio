name: dio_browser_adapter
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.0.0
  meta: ^1.5.0

dev_dependencies:
  lints: ^2.0.0
  flutter_test:
    sdk: flutter
  
  # Shared test package.
  dio_test:
    git:
      url: https://github.com/cfug/dio
      path: dio_test

# dependency_overrides:
#   dio:
#     path: ../../dio

platforms:
  web: